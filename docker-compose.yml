version: '3.8'

services:
  python-service:
    build: ./python-service
    container_name: python-service
    restart: unless-stopped
    expose:
      - "5000"
    networks:
      - n8n_default

networks:
  n8n_default:
    external: true
